import styled from "styled-components"

const ContainerHeader = styled.div`
    padding: 0.5rem;
    background-color: #C7E3EE;
    display: flex;
`

const Menu = styled.ul`
    list-style-type: nome;
    display: flex;
`
const ItensMenu = styled.ul`
`
export { ContainerHeader, Menu, ItensMenu}